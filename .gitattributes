# Force LF line endings for shell scripts (required for Docker/Linux containers)
*.sh text eol=lf

# Force LF for other common Unix files
*.bash text eol=lf
*.zsh text eol=lf
*.list text eol=lf
*.conf text eol=lf

# Let Git handle line endings for other text files automatically
* text=auto
