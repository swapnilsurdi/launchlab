# LaunchLab Environment Configuration Template
# Copy this file to .env and configure your values
# DO NOT commit .env to git (contains secrets)

# ==============================================
# USER CONFIGURATION (Set during quicksetup)
# ==============================================

# Admin email (used for Immich, Paperless, notifications)
EMAIL=admin@homelab.local

# ==============================================
# VPN CONFIGURATION
# ==============================================

# VPN type: "wireguard" or "tailscale"
VPN_TYPE=wireguard

# Docker Compose profile to activate
COMPOSE_PROFILES=wireguard

# ==============================================
# WIREGUARD + DUCKDNS (if VPN_TYPE=wireguard)
# ==============================================

# WireGuard VPN password (custom, set by user)
WG_PASSWORD_HASH=your_bcrypt_hash_here

# DuckDNS Dynamic DNS (free at https://duckdns.org)
DUCKDNS_DOMAIN=yourdomain
DUCKDNS_TOKEN=your-duckdns-token-here
DOMAIN=${DUCKDNS_DOMAIN}.duckdns.org

# ==============================================
# TAILSCALE (if VPN_TYPE=tailscale)
# ==============================================

# Tailscale auth key (get at https://login.tailscale.com/admin/settings/keys)
TAILSCALE_AUTHKEY=tskey-auth-xxxxx-xxxxxx
TAILSCALE_HOSTNAME=launchlab

# Tailscale API automation (optional but recommended)
# Get API token at: https://login.tailscale.com/admin/settings/keys
# Required scopes: Devices (Write), Routes (Write), DNS (Write)
TAILSCALE_API_TOKEN=tskey-api-xxxxx
TAILSCALE_TAILNET=yourname@gmail.com

# ==============================================
# AUTO-DETECTED (Confirmed during setup)
# ==============================================

# Timezone (auto-detected from system)
TIMEZONE=America/Los_Angeles

# Local network subnet (auto-detected from routing table)
LOCAL_SUBNET=192.168.1.0/24

# ==============================================
# BACKEND SERVICES (Auto-generated strong passwords)
# ==============================================

# PostgreSQL shared database
POSTGRES_USER=homelab
POSTGRES_PASSWORD=auto_generated_secure_password_here

# Redis shared cache
REDIS_PASSWORD=auto_generated_secure_password_here

# ==============================================
# USER-FACING SERVICES
# ==============================================

# Default admin password for all services (minimum 12 characters)
# IMPORTANT: Change this after first login!
ADMIN_PASSWORD=changeme12345

# Portainer container management
PORTAINER_ADMIN_PASSWORD=${ADMIN_PASSWORD}

# Jellyfin media server
JELLYFIN_ADMIN_PASSWORD=${ADMIN_PASSWORD}

# Paperless-ngx document management
PAPERLESS_ADMIN_USER=admin
PAPERLESS_ADMIN_PASSWORD=${ADMIN_PASSWORD}
PAPERLESS_ADMIN_EMAIL=${EMAIL}
PAPERLESS_SECRET_KEY=auto_generated_64_char_hex_key

# Pi-hole DNS + ad blocking
PIHOLE_PASSWORD=${ADMIN_PASSWORD}

# ==============================================
# SERVICE-SPECIFIC SECRETS
# ==============================================

# Random secret keys (auto-generated)
WEBUI_SECRET_KEY=auto_generated_64_char_hex_key

# ==============================================
# MATRIX CONFIGURATION
# ==============================================

# Matrix server name (used in user IDs: @user:SERVER_NAME)
MATRIX_SERVER_NAME=homelab.local

# Matrix federation (disable for private homelab)
MATRIX_ENABLE_FEDERATION=false

# ==============================================
# NOTES
# ==============================================

# This file is auto-generated by scripts/quicksetup.sh
# Manual editing is NOT recommended - re-run quicksetup instead
#
# Security:
# - All services use default password "changeme12345" (except WireGuard)
# - IMPORTANT: Change ADMIN_PASSWORD after first login!
# - Services are VPN-protected, not publicly exposed
#
# Backup this file securely - it contains all credentials
